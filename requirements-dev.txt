# Development / test pinned dependencies for local test runs
# These pins are chosen to match the working test environment for this repo.
fastapi==0.95.2
starlette==0.27.0
httpx==0.23.3
pydantic==1.10.24
uvicorn==0.22.0
pytest==7.4.0
python-multipart==0.0.6
requests==2.32.3
# Pull in the core backend dependency set first
-r backend/requirements.txt

# HTTP client + ASGI helpers
httpx==0.27.2
starlette==0.38.2

# Code quality tooling
black==24.8.0
flake8==7.1.1

# Optional: install with `pip install -r requirements-dev.txt`
