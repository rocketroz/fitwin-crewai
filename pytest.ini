[pytest]
# Only collect tests under the tests/ directory to avoid running repo-level
# example or CI files that may require credentials or external services.
testpaths = tests
python_files = test_*.py
addopts = -q
