# Development / test pinned dependencies
# These requirements layer on top of the backend stack defined in backend/requirements.txt.

# Pull in the core backend dependency set first (FastAPI 0.115.x, Pydantic v2, etc.)
-r backend/requirements.txt

# HTTP client helpers for local testing / CLI usage
httpx==0.27.2

# Code quality tooling
black==24.8.0
flake8==7.1.1
